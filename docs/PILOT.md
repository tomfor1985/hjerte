# Initial pilot — 6 September 2026

50 published questions across 15 chapters; 25 held for optional review. Published mix: 19 direct knowledge questions, 17 cases and 14 interpretation questions. Two primary guidelines and five supporting study-note files were imported. This is an initial bank, not complete EAPC curriculum coverage. The 140-question exam remains unavailable until the bank contains at least 140 distinct published questions.

Estimated cost from reported tokens: 78.8398 NOK, using the documented conservative conversion and tax assumptions. A further 20.5245 NOK remains reserved for an initial connection failure whose billing could not be confirmed. Accounted cost plus reservations: 99.3643 NOK of the approved 200 NOK. Remaining allowance: 100.6357 NOK. No automatic top-up; practice itself incurs no API cost.

The independent reviewer approved answers without seeing the author's answer index, followed by a separate explanation check. All published questions passed exact source-quotation checks. Held questions include citation-matching failures and review disagreements; they are excluded from practice. Automated checks reduce errors but do not guarantee medical accuracy.

Initial pilot provenance conservatively lists linked notes where exact excerpt selection was not retained. Subsequent generation records the supplied note pages and hashes. Every pilot question retains its primary guideline, year, DOI, file hash and generation job.

Verified: 44 automated tests, GitHub checks, VPS container health, HTTPS, authenticated tomas login, private source access, main routes and PWA assets. The production backup was restored into a separate temporary directory; database integrity and all seven source-file hashes matched. No study answers were created by the live checks. Responsive layout is implemented; broad browser visual QA was not requested.

Production is the authoritative API allowance ledger. Local paid generation is disabled. Nightly backups retain 30 archives in the separate Hjerte backup volume. Local credentials are in the ignored, permission-restricted data/LOGIN.txt file; secrets and the question bank are excluded from Git.
